# Clawso Labs Visual Assets

Place the approved desktop hero image here:

```text
assets/command-bridge-hero.png
```

The homepage already references that path. Use the cockpit image with the back-facing operator, segmented cockpit frame, and warm enterprise-network planet outside.
